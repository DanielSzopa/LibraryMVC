@model LibraryMVC.Application.AuthorListVm

@{
    ViewData["Title"] = "Authors";
    ViewData["Controller"] = "Book";
    ViewData["Action"] = "ViewAuthors";
}

<h1>Authors</h1>

<p>
    <a asp-controller="Book" asp-action="AddNewBook">Create Book</a>
    |
    <a asp-controller="Book" asp-action="Index">Books</a>
    |
    <a asp-controller="Book" asp-action="ViewCategories">Categories</a>
    |
    <a asp-controller="Book" asp-action="ViewTypeOfBooks">TypeOfBooks</a>
    |
    <a asp-controller="Book" asp-action="ViewPublishers">Publishers</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
               FirstName
            </th>
            <th>
               LastName
            </th>
            <th>
              Books
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Authors)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumberOfBooks)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "AuthorDetails", new { item.Id }) |
                    @Html.ActionLink("Delete", "DeleteAuthor", new { item.Id })
                </td>
            </tr>

        }       
    </tbody>
</table>

<partial name="_PaginationPartial" view-data="ViewData" model="Model" />